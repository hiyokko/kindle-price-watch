{
  "name": "kindle-price-watch",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "dev": "node server.mjs",
    "check": "node scripts/check-once.mjs",
    "test": "node --test",
    "deploy:production": "node scripts/deploy-production.mjs"
  },
  "dependencies": {
    "@vercel/blob": "^2.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
