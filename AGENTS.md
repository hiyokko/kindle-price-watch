# User Preferences

- When saving files or artifacts, use `/Users/ponpokotanuki/Desktop/codex` as the default destination unless the user specifies another path or the current project requires files to stay inside the repository.
- For any change that affects this project, including code, configuration, documentation, and these instructions, do not stop at local edits. Even if the user does not give a separate deployment instruction, commit the completed changes to GitHub and deploy the production Vercel app unless the user explicitly says not to. Afterward, verify both GitHub and Vercel are updated.
- When updating this project, keep every related copy or target in sync: local files in this repository, the GitHub `main` branch, and the production Vercel deployment. Do not treat a change as complete while any applicable local, GitHub, or Vercel target is still stale.
- Keep Vercel Git auto-deploy integration disabled for this project. Use GitHub for source sync, then deploy production explicitly after pushing `main`.
- Deploy production with `node scripts/deploy-production.mjs`, not `vercel deploy --prod` from the repository root. The deploy script uploads from a temporary copy without `.git`, local `.env*`, `node_modules`, or local `data/*.json`, which avoids private GitHub metadata and local data leaking into Vercel deployments.
- GitHub Actions logs can be inspected with `node scripts/github-actions.mjs`. The GitHub token is stored in macOS Keychain under service `kindle-price-watch-github-actions`, account `hiyokko`. If the normal sandbox cannot read Keychain, rerun the same `node scripts/github-actions.mjs ...` command with escalated permission instead of asking the user to issue or paste another token.
- For GitHub Actions access, first run `node scripts/github-actions.mjs runs --limit 20 --json` to confirm API access and identify the run id, then use `node scripts/github-actions.mjs jobs <run-id> --json` and `node scripts/github-actions.mjs logs <run-id>` for details. The token was verified working against the private `hiyokko/kindle-price-watch` repository on 2026-05-13. Only ask the user to recreate the token if the Keychain item is missing or GitHub returns an authentication/authorization error after an escalated retry.
- To store/update the Actions token, use `node scripts/save-github-actions-token.mjs` only after the user has copied a fine-grained PAT scoped to this repository with `Actions: Read-only` and `Metadata: Read-only`. Never print the token value.
- Keep `BLOB_READ_WRITE_TOKEN` scoped to Vercel Production only. Do not add the production Blob token to Preview or Development unless those environments use an isolated Blob store or `BLOB_STORE_PATH`.
- Price repair and other bulk maintenance must never write Vercel Blob once per book. Fetch/check individual Amazon pages as needed, but accumulate the results in memory and persist with a single `updateStore` call at the end of the batch. Do not run repair workflows through `checkBookById` in a loop, because that performs one Blob write per book.
- When a change benefits from reorganizing files inside the project folders, handle the file moves as part of the work. Update all affected imports, Vercel/API routes, scripts, documentation, and verification steps so the moved files work in local, GitHub, and production Vercel contexts.
- When verifying production, use `https://kindle-price-watch.vercel.app/` and check the relevant API/UI behavior after deployment, not only the Vercel deploy command result.
- Keep durable project lessons and incident-prevention rules in this `AGENTS.md` as part of future work, even when the user does not repeat that instruction. Add only verified, reusable guidance rather than transient debugging notes.
- Treat a series source ASIN and `seriesKey` as a stable identity. Never replace its stored work title merely because a fetched candidate has more books. Merge candidates only when their child ASINs overlap or their normalized series names agree; otherwise retain the stored series, mark the refresh as deferred, and investigate.
- When a series group contains conflicting titles, do not use book-count majority alone to decide which title survives. Prefer a coherent identity that was registered earlier, and preserve data when identity evidence is ambiguous. A later, larger candidate can be a contaminated fallback result.
- Before repairing production Blob data, confirm no GitHub Actions price-check run is active. After it stops, reread the latest Blob and apply the entire repair in one synchronous `updateStore` mutation so conflict retries remain available and no completed run can overwrite the repair.
- For silent title-change audits, compare by both child ASIN and stable series source key. Classify punctuation, label, volume-marker, and full-width differences separately from a disjoint child-ASIN set under a different work name; only the latter is a series identity replacement.
- A new volume can be discovered after the cron price-check plan has already been built, during per-checked-series discovery. If that new volume still has no trusted price, promote it into the current run's remaining checks instead of leaving it unresolved until the next schedule; keep the promotion bounded and preserve the runtime/save reserve. Persist release dates returned by preorder pages during both successful and metadata-only checks.
